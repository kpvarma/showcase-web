{
  "build": {
    "command": "npm run build",
    "env": {
      "NODE_ENV": "production"
    }
  },
  "includeFiles": [
    "data/**", // Include the directory containing your MDX files
    ".contentlayer/**" // Ensure Contentlayer's output is available
  ]
}